{
  "cloudspace_name": "${NAME}",
  "region": "${REGION}",
  "node_pools": {
    "default": {
      "server_class": "${SERVER_CLASS}",
      "bid_price": ${BID_PRICE},
      "min_nodes": ${MIN_NODES},
      "max_nodes": ${MAX_NODES}
    }
  },
  "hacontrol_plane": ${HA_CONTROL_PLANE}
}
